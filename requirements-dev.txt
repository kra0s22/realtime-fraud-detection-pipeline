# Dev/test dependencies for running the pytest suite directly on the CI runner
# (no Docker image build). Mirrors the packages installed by docker/spark and
# docker/test so the same environment is reproduced outside containers.
pyspark==3.5.1
delta-spark==3.2.0
deltalake==0.19.0
redis==5.2.1
pydantic==2.10.4
pydantic-settings==2.7.1
fastapi==0.115.6
httpx
mlflow==2.17.2
confluent-kafka==2.4.0
scikit-learn==1.5.2
pytest
fakeredis
